name: Deploy to cPanel

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run — list changes without uploading'
        required: false
        default: false
        type: boolean

jobs:
  deploy:
    name: FTP Deploy to cPanel
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Stamp service worker cache version with commit SHA
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)
          sed -i "s/const CACHE_VERSION = '[^']*'/const CACHE_VERSION = '${SHORT_SHA}'/" sw.js

      - name: Generate env.js from secrets
        run: |
          cat > js/config/env.js <<'ENVEOF'
          window.SUPABASE_URL      = '${{ secrets.SUPABASE_URL }}';
          window.SUPABASE_ANON_KEY = '${{ secrets.SUPABASE_ANON_KEY }}';
          window.ENV = {
            SUPABASE_URL:      window.SUPABASE_URL,
            SUPABASE_ANON_KEY: window.SUPABASE_ANON_KEY,
            ready: !!(window.SUPABASE_URL && window.SUPABASE_ANON_KEY),
          };
          window.__APP_READY__ = false;
          ENVEOF

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: ${{ secrets.FTP_PORT }}
          protocol: ftps
          local-dir: ./
          server-dir: ./public_html/
          dry-run: ${{ inputs.dry_run || false }}
          exclude: |
            **/.git*
            **/.git*/**
            .github/**
            node_modules/**
            .claude/**
            tests/**
            serve.js
            deploy.js
            package.json
            package-lock.json
            CLAUDE.md
            **/*.test.js
